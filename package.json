{
  "name": "constructionPlanMgmt",
  "version": "1.0.0",
  "description": "construction plan managment app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "install": "cd client && yarn install",
    "build": "cd client && yarn run build",
    "heroku-postbuild": "yarn run build"
  },
  "repository": "git@github.com:gracemiller23/constructionPlanMgmt.git",
  "author": "Grace Miller",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "mongoose": "^5.2.3"
  }
}
